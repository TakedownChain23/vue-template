export class Pineapple {
    name: string;
    weight: number;
    ripe: boolean;

    constructor(name: string, weight: number, ripe: boolean) {
        this.name = name;
        this.weight = weight;
        this.ripe = ripe;
    }
}